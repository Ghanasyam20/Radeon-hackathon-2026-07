# Infrastructure

Future deployment/runtime configuration lives here.

Planned areas:

- `rocm/` - AMD ROCm worker setup
- `vercel/` - deployment notes/configuration
- `docker/` - local container tooling

The web experience and GPU compute plane are intentionally separated.
